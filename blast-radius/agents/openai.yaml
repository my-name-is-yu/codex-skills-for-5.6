interface:
  display_name: "Blast Radius"
  short_description: "Prove what a change could break beyond its diff"
  default_prompt: "Use $blast-radius to trace and prove the risks of this change."
policy:
  allow_implicit_invocation: false
